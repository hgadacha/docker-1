docker container run -d --name=roach-warden -p 8081:80 --link spawning-pool:db phpmyadmin/phpmyadmin
#
#
# (-d)(--detach) : Execute le container en arriere plan.
#
# (--name) : Attribue un nom au conteneur.
#
# (-p)(--publish) : Publie (lie) les ports d'un conteneur sur l'hôte.
#
# (--link) : --link : Ajoute un lien vers un autre conteneur. Attend 2 arguments separes par (:),
# le premier argument doit etre un ID ou le nom d'un conteneur avec qui le lien veut etre
# etablie, le second argument correspond au nom du service.
#
# phpMyAdmin (PMA) est une application Web de gestion pour les systèmes de gestion de base
# de données MySQL réalisée principalement en PHP et distribuée sous licence GNU GPL.
#
# liens utiles :
# https://hub.docker.com/r/phpmyadmin/phpmyadmin
